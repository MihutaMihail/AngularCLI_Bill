import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Import your component

export const routes: Routes = [
  { path: '', component: AppComponent } // Define a route
];
