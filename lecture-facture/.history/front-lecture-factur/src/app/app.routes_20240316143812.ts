import { Routes } from '@angular/router';
import { PhotoUploadComponent } from './photo-upload/photo-upload.component'; // Update the import path

export const routes: Routes = [
  { path: '', component: PhotoUploadComponent } // Define a route for the PhotoUploadComponent
];
